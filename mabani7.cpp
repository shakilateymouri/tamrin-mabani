#include(iostream.h)
main()
{
    int A[100];
    A=readArray(A,100);
    A=sort(A);
    cin>>t;
    k=search(A,t);
    int*readArray(int*A,int;)
    {
        int j;
        for(j=0,j<i,j++)
        cin>>A[j]
        return A;
    }
}