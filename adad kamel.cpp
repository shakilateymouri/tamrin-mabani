#include (iostream.h)
 main()
 {
    cin>>a>>b;
    if(a>b)
    {
        temp=a;
        a=b;
        b=temp
    }
    for(n=a+i ; n<b; n++)
    {
        sum==o
        for (i=1,i<=n;i++)
          if in%i == 0
          sum = i
          if(sum==z*n)
          cout<<n;
    }
 }