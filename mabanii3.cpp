#include(iostream.h)
main()
{
    int maximum(inta,intb,intc)
    {
        int max;
        Maxsa
        if (Max<b)
         Max.b;
        if(Max<c)
          Max=c;
          return Max;

    }
}