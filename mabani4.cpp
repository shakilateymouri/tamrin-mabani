#include(iostream.h)
main()
{
    intx,y,z;
    x=5y=2
    z=p plus(x,y);
    cout<<2;
}
int p plus (int x,y)
{
    if(y==0) return x+5;
    if(y>0) return plus(x+1,y-1)+5;
    if(y<0)return plus(x-1,y+1)+5:
}